<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:resultsVm="clr-namespace:LabelVal.Results.ViewModels"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d">

    <!--  Results Databases  -->
    <DataTemplate x:Key="ImageRdMenuItem" DataType="{x:Type resultsVm:ResultsDatabasesViewModel}">
        <!-- The DataContext here is an instance of ResultsDatabasesViewModel -->
        <DockPanel d:DataContext="{d:DesignInstance Type=resultsVm:ResultsDatabasesViewModel}" DataContext="{Binding Content}">
            <!-- The DataContext is now the 'Content' property, which is likely null at design time -->

            <DockPanel Width="48" Background="Purple">

                <iconPacks:PackIconMaterial
                    Width="36"
                    Height="36"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Kind="StoreOutline" />

            </DockPanel>

            <Border Style="{StaticResource imageBar}" />

            <md:Card Margin="3" Background="{DynamicResource MahApps.Brushes.Control.Background}">
                <DockPanel
                    Margin="3"
                    HorizontalAlignment="Stretch"
                    Background="{DynamicResource MahApps.Brushes.ThemeBackground}">

                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
                        </Style>
                    </DockPanel.Style>

                    <Border
                        Margin="0,0,6,0"
                        Background="{DynamicResource MahApps.Brushes.Accent2}"
                        Style="{StaticResource groupLeft}" />
                    <Border
                        Margin="-6,3,0,0"
                        Background="{DynamicResource MahApps.Brushes.Accent2}"
                        Style="{StaticResource groupBottom}" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <iconPacks:PackIconMaterial
                            Margin="0,3,6,0"
                            VerticalAlignment="Center"
                            Kind="Database" />

                        <!--<TextBlock
                        Grid.Row="0"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style BasedOn="{StaticResource MahApps.Styles.TextBlock.HamburgerMenuHeader}" TargetType="TextBlock">
                                <Setter Property="Text" Value="{Binding SelectedResultsDatabase.File.Name}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedResultsDatabase, Converter={StaticResource IsNullOrEmpty}}" Value="True">
                                        <Setter Property="Text" Value="NO DATABASE SELECTED" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>-->

                        <Grid
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="3,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>

                            <md:SmartHint
                                Grid.Column="0"
                                Margin="3,0"
                                FontSize="14"
                                Hint="...">
                                <md:SmartHint.Style>
                                    <Style TargetType="md:SmartHint">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding RightAlignOverflow}" Value="True" />
                                                    <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=srcName3}" Value="Visible" />
                                                </MultiDataTrigger.Conditions>
                                                <MultiDataTrigger.Setters>
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </MultiDataTrigger.Setters>
                                            </MultiDataTrigger>

                                        </Style.Triggers>
                                    </Style>
                                </md:SmartHint.Style>
                            </md:SmartHint>

                            <ScrollViewer
                                x:Name="srcName3"
                                Grid.Column="1"
                                Padding="3,0"
                                VerticalAlignment="Center"
                                md:ScrollViewerAssist.BubbleVerticalScroll="True"
                                md:ScrollViewerAssist.IsAutoHideEnabled="True"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Auto"
                                VerticalScrollBarVisibility="Disabled">
                                <ScrollViewer.Resources>
                                    <Style TargetType="ScrollBar" />
                                    <sys:Double x:Key="{x:Static SystemParameters.HorizontalScrollBarHeightKey}">8</sys:Double>
                                </ScrollViewer.Resources>

                                <ScrollViewer.Style>
                                    <Style BasedOn="{StaticResource MaterialDesignScrollViewer}" TargetType="ScrollViewer">
                                        <Setter Property="FlowDirection" Value="LeftToRight" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RightAlignOverflow}" Value="True">
                                                <Setter Property="FlowDirection" Value="RightToLeft" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ScrollViewer.Style>


                                <TextBlock Text="{Binding SelectedResultsDatabase.File.Name}">
                                    <TextBlock.Style>
                                        <Style BasedOn="{StaticResource MaterialDesignBodyMediumTextBlock}" TargetType="TextBlock">
                                            <Setter Property="FlowDirection" Value="LeftToRight" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RightAlignOverflow}" Value="True">
                                                    <Setter Property="FlowDirection" Value="RightToLeft" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </ScrollViewer>

                            <md:SmartHint
                                Grid.Column="2"
                                Margin="3,0"
                                FontSize="14"
                                Hint="...">
                                <md:SmartHint.Style>
                                    <Style TargetType="md:SmartHint">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding RightAlignOverflow}" Value="False" />
                                                    <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=srcName3}" Value="Visible" />
                                                </MultiDataTrigger.Conditions>
                                                <MultiDataTrigger.Setters>
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </MultiDataTrigger.Setters>
                                            </MultiDataTrigger>

                                        </Style.Triggers>
                                    </Style>
                                </md:SmartHint.Style>
                            </md:SmartHint>

                        </Grid>

                        <DockPanel
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="0,0,0,3"
                            HorizontalAlignment="Left">
                            <iconPacks:PackIconMaterial
                                Margin="3"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                                <iconPacks:PackIconMaterial.Style>
                                    <Style TargetType="{x:Type iconPacks:PackIconMaterial}">
                                        <Setter Property="Kind" Value="LockOpenVariant" />
                                        <Setter Property="ToolTip" Value="Database is unlocked." />

                                        <Style.Triggers>

                                            <DataTrigger Binding="{Binding SelectedResultsDatabase.IsLocked}" Value="True">
                                                <Setter Property="Kind" Value="Lock" />
                                                <Setter Property="ToolTip" Value="Database is locked." />
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding SelectedResultsDatabase.IsPermLocked}" Value="True">
                                                <Setter Property="Kind" Value="DatabaseLock" />
                                                <Setter Property="ToolTip" Value="Database is premanently locked." />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </iconPacks:PackIconMaterial.Style>
                            </iconPacks:PackIconMaterial>
                        </DockPanel>

                    </Grid>

                </DockPanel>
            </md:Card>
        </DockPanel>
    </DataTemplate>

</ResourceDictionary>