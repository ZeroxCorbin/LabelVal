<UserControl
    x:Class="LabelVal.Main.Views.StatusMessageViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Main.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:LabelVal.Main.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:SystemMessagesDataContext}"
    mc:Ignorable="d">

    <UserControl.Resources />

    <DockPanel>
        <!--<ScrollViewer VerticalScrollBarVisibility="Auto">-->


        <DataGrid
            x:Name="dataGrid"
            MaxHeight="400"
            AutoGenerateColumns="False"
            ItemsSource="{Binding Path=FilteredMessagesView, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Style="{StaticResource MahApps.Styles.DataGrid.Azure}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Binding="{Binding TimeStamp, Mode=OneTime}"
                    Header="Timestamp"
                    HeaderStyle="{StaticResource MahApps.Styles.DataGridColumnHeader.Azure}"
                    SortDirection="Descending" />
                <DataGridTextColumn
                    Binding="{Binding Message, Mode=OneTime}"
                    Header="Message"
                    HeaderStyle="{StaticResource MahApps.Styles.DataGridColumnHeader.Azure}" />
                <DataGridTextColumn
                    Binding="{Binding Value, Mode=OneTime}"
                    Header="Type"
                    HeaderStyle="{StaticResource MahApps.Styles.DataGridColumnHeader.Azure}" />
                <DataGridCheckBoxColumn
                    Binding="{Binding HasException, Mode=OneTime}"
                    Header="Has Exception"
                    HeaderStyle="{StaticResource MahApps.Styles.DataGridColumnHeader.Azure}" />
            </DataGrid.Columns>
        </DataGrid>
        <!--</ScrollViewer>-->
    </DockPanel>
</UserControl>
