<UserControl
    x:Class="LabelVal.Main.Views.AppSettingsControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converters="clr-namespace:LabelVal.Converters"
    xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    xmlns:V275Converters="clr-namespace:LabelVal.V275.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Main.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:LabelVal.Main.ViewModels"
    Dialog:DialogParticipation.Register="{Binding}"
    d:DataContext="{d:DesignInstance Type=viewModels:GlobalAppSettings}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Center">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                Text="Show Button Text" />

            <ToggleButton IsChecked="{Binding ShowButtonText}" />

        </StackPanel>

        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Margin="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                Text="Launch LVS on Connect" />

            <ToggleButton IsChecked="{Binding LvsLaunchOnConnect}" />

        </StackPanel>

        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                Text="Ignore LVS Image&#x0a;on No Decode" />

            <ToggleButton IsChecked="{Binding LvsIgnoreNoResults}" />

        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Margin="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                Text="Auto Detect V275 Nodes" />

            <ToggleButton IsChecked="{Binding V275AutoRefreshServers}" />

        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                Text="Preserve Image&#x0a;Format" />

            <ToggleButton IsChecked="{Binding PreseveImageFormat}" />

        </StackPanel>

    </Grid>
</UserControl>
