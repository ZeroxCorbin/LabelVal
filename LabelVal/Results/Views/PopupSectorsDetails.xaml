<UserControl
    x:Class="LabelVal.Results.Views.PopupSectorsDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Results.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:sectors="clr-namespace:LabelVal.Sectors.Views"
    mc:Ignorable="d">

    <UserControl.Resources>
        <CollectionViewSource x:Key="cvsStoredSectors" Source="{Binding}">
            <CollectionViewSource.SortDescriptions>
                <componentmodel:SortDescription Direction="Ascending" PropertyName="Template.CenterPoint.X" />
                <!--<componentmodel:SortDescription PropertyName="Template.Top" Direction="Ascending"/>-->
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Popup x:Name="Popup" StaysOpen="False">
        <Popup.Style>
            <Style TargetType="{x:Type Popup}" />
        </Popup.Style>

        <GroupBox
            Margin="3"
            Padding="3"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            md:ColorZoneAssist.Background="{DynamicResource MahApps.Brushes.Accent4}"
            md:ColorZoneAssist.Mode="Custom"
            Style="{StaticResource MaterialDesignCardGroupBox}">

            <GroupBox.Header>
                <DockPanel Margin="-5">

                    <Button
                        Margin="6,0,3,0"
                        Click="btnSaveImage_Click"
                        Style="{StaticResource GroupHeaderCirlceButton}"
                        ToolTip="Save as image.">
                        <iconPacks:PackIconMaterial
                            Width="14"
                            Height="14"
                            Kind="ContentSave" />
                    </Button>

                    <Button
                        Click="btnCopyImage_Click"
                        Style="{StaticResource GroupHeaderCirlceButton}"
                        ToolTip="Copy image to clipboard as PNG.">
                        <iconPacks:PackIconMaterial
                            Width="14"
                            Height="14"
                            Kind="ContentCopy" />
                    </Button>

                </DockPanel>

            </GroupBox.Header>

            <DockPanel>

                <ScrollViewer HorizontalScrollBarVisibility="Auto" Style="{StaticResource MahApps.Styles.ScrollViewer}">

                    <ItemsControl x:Name="itemsControl" ItemsSource="{Binding Source={StaticResource ResourceKey=cvsStoredSectors}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel MaxWidth="1400" IsItemsHost="True" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <sectors:SectorDetails Foreground="{DynamicResource MahApps.Brushes.Text}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </DockPanel>
        </GroupBox>
    </Popup>
</UserControl>
