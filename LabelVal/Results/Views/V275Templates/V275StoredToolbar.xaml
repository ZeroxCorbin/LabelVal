<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Results.Views"
    xmlns:vm="clr-namespace:LabelVal.Results.ViewModels">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Results/Views/Shared/ResultsToolbarShared.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Stored Toolbar (V275)  -->
    <DockPanel x:Key="V275StoredToolbar" x:Shared="False">

        <!--  Both: Copy  -->
        <Button
            Command="{Binding CopyToClipboardCommand, RelativeSource={RelativeSource AncestorType=local:ResultsDeviceEntry_V275}}"
            CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
            Content="{iconPacks:Modern Kind=PageCopy}">
            <Button.Style>
                <Style BasedOn="{StaticResource ResultsToolbarButtonBase}" TargetType="Button">
                    <Setter Property="Tag" Value="{Binding StoredSectors}" />
                    <Setter Property="ToolTip" Value="Copy the sectors to the clipboard as CSV." />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=Tag, RelativeSource={RelativeSource AncestorType=ContentControl}}" Value="Image">
                            <Setter Property="Tag" Value="{Binding StoredImage}" />
                            <Setter Property="ToolTip" Value="Copy the image to the clipboard as a bitmap." />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!--  Image Only: Save stored image  -->
        <Button
            Command="{Binding ResultsEntry.SaveCommand}"
            CommandParameter="{Binding StoredImage.BitmapBytes}"
            Style="{StaticResource SaveImageButtonStyle}">
            <Button.ToolTip>Save the stored image to a file.</Button.ToolTip>
        </Button>

        <!--  Sectors Only: Load (retain original multi-condition visibility)  -->
        <DockPanel>
            <DockPanel.Style>
                <Style TargetType="DockPanel">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ResultsManagerView.SelectedV275Node, Mode=OneWay}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ResultsManagerView.SelectedV275Node.Controller.IsLoggedIn_Control}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ResultsManagerView.SelectedV275Node.Controller.State}" Value="Running">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ResultsManagerView.SelectedV275Node.Controller.State}" Value="Idle">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=Tag, RelativeSource={RelativeSource AncestorType=ContentControl}}" Value="Image">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Style>

            <Button
                Command="{Binding LoadCommand}"
                Content="{iconPacks:Material Kind=Download}"
                Style="{StaticResource ResultsToolbarButtonBase}"
                ToolTip="Overwrite the template sectors with the stored data from the database." />

            <Border Margin="1,0" Style="{StaticResource imageBar}" />
        </DockPanel>



        <!--  Sectors Only: Show All  -->
        <Button
            Command="{Binding ShowStoredSectorsCommand, RelativeSource={RelativeSource AncestorType=local:ResultsDeviceEntry_V275}}"
            CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
            Style="{StaticResource ShowAllButtonStyle}" />

        <!--  Sectors Only: JSON  -->
        <Button
            Command="{Binding ShowStoredSectorsCommand, RelativeSource={RelativeSource AncestorType=local:ResultsDeviceEntry_V275}}"
            CommandParameter="{Binding Tag, RelativeSource={RelativeSource Self}}"
            Style="{StaticResource JsonButtonStyle}" />

        <!--  Image Only: Delete stored (with separator & DB lock rule already in shared DeleteStoredButtonStyle)  -->
        <DockPanel>
            <DockPanel.Style>
                <Style TargetType="DockPanel">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=Tag, RelativeSource={RelativeSource AncestorType=ContentControl}}" Value="Sectors">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ResultsManagerView.SelectedResultsDatabase.IsLocked}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Style>

            <Border Margin="1,0" Style="{StaticResource imageBar}" />

            <Button
                Command="{Binding ClearStoredCommand}"
                CommandParameter="{x:Static vm:ResultsEntryDevices.V275}"
                Style="{StaticResource DeleteStoredButtonStyle}" />
        </DockPanel>
    </DockPanel>
</ResourceDictionary>