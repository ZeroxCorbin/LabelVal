<UserControl x:Class="LabelVal.Results.Views.ImageResultEntry"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:LabelVal.Results.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
            xmlns:ViewModels="clr-namespace:LabelVal.Results.ViewModels"
            xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
            Dialog:DialogParticipation.Register="{Binding}"
            d:DataContext="{d:DesignInstance Type=ViewModels:ImageResultEntry}"
            mc:Ignorable="d" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <DockPanel>

        <DockPanel>
            <GroupBox Margin="3" Padding="3" HorizontalAlignment="Left" VerticalAlignment="Stretch" 
                  Style="{StaticResource MaterialDesignCardGroupBox}"
                md:ColorZoneAssist.Background="{DynamicResource MahApps.Brushes.Accent4}"
                md:ColorZoneAssist.Mode="Custom">

                <GroupBox.Header>
                    <DockPanel Margin="-5">
                        <!--Title-->
                        <md:Card Background="{DynamicResource MahApps.Brushes.ThemeBackground}" HorizontalAlignment="Stretch" Padding="4,1">
                            <TextBlock Text="{Binding SourceImage.Order}" MinWidth="26" FontSize="{DynamicResource MahApps.Font.Size.ContextMenu}"
                                       HorizontalAlignment="Center"/>
                        </md:Card>
                    </DockPanel>
                </GroupBox.Header>

                <StackPanel>
                     
                    <Button VerticalAlignment="Top" BorderThickness="0" Margin="0,3"
                            Content="{iconPacks:Material Kind=ArrowUpBoldBox}"
                            ToolTip="Move Image Up" md:HintAssist.Hint="Move" md:HintAssist.IsFloating="True">

                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SourceImage.Order}" Value="1">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedImageRoll.IsLocked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button VerticalAlignment="Top" BorderThickness="0" Margin="0,3"
                            Content="{iconPacks:Material Kind=ArrowDownBoldBox}"
                            ToolTip="Move Image Down">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedImageRoll.IsLocked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Separator>
                        <Separator.Style>
                             <Style TargetType="Separator">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedImageRoll.IsLocked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Separator.Style>
                    </Separator>

                    <Button VerticalAlignment="Top" BorderThickness="0" Margin="0,3"
                            Content="{iconPacks:Material Kind=ImageAreaClose, RotationAngle=180}"
                            ToolTip="Add Image Above">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedImageRoll.IsLocked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button VerticalAlignment="Top" BorderThickness="0" Margin="0,3"
                            Content="{iconPacks:Material Kind=ImageAreaClose}"
                            ToolTip="Add Image Below">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedImageRoll.IsLocked}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>
            </GroupBox>
        </DockPanel>

        <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" mah:ScrollViewerHelper.BubbleUpScrollEventToParentScrollviewer="True">
            <DockPanel>

                <local:ImageResultEntry_Images DataContext="{Binding }" DockPanel.Dock="Top"/>

                <local:ImageResultEntry_V275 DataContext="{Binding }"/>

                <local:ImageResultEntry_V5 DataContext="{Binding }"/>

                <local:ImageResultEntry_L95xx DataContext="{Binding }"/>
            </DockPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
