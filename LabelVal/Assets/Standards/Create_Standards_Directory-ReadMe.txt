1. Create a directory in the "\UserData\Standards" directory to contain your images.
	-Keep the name short. It will be displayed in LabelVal Standards pulldown.
	-If the name starts with "gs1" then LabelVal will use V275 Edit mode to process the images.
	