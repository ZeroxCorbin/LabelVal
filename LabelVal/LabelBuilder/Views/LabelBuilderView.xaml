<mah:MetroWindow
    x:Class="LabelVal.LabelBuilder.Views.LabelBuilderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:barcodeViews="clr-namespace:BarcodeBuilder.lib.Wpf.Views;assembly=BarcodeBuilder.lib.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.LabelBuilder.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:vm="clr-namespace:LabelVal.LabelBuilder.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:LabelBuilderViewModel}"
    Icon="/favicon.ico"
    SaveWindowPosition="True"
    mc:Ignorable="d">

    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <Image
                Width="24"
                Height="24"
                Margin="4,0,0,0"
                Source="{Binding}" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>

    <mah:MetroWindow.DataContext>
        <vm:LabelBuilderViewModel />
    </mah:MetroWindow.DataContext>

    <mah:MetroWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Templates/SharedButtonStyles.xaml" />
                <ResourceDictionary Source="/LabelBuilder/Templates/LabelBuilderSectionContainerStyle.xaml" />
                <ResourceDictionary Source="/LabelBuilder/Templates/LabelBuilderSectionExpanderStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </mah:MetroWindow.Resources>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>

            <DockPanel>
                <!--<TextBlock
                    Margin="3,0"
                    VerticalAlignment="Center"
                    Text="{Binding ColorBlindnessType, Converter={StaticResource EnumValueToDescription}}" />-->

                <Button
                    x:Name="btnColorBlind"
                    BorderThickness="0"
                    Click="btnColorBlind_Click">
                    <iconPacks:PackIconModern
                        Width="20"
                        Height="20"
                        Kind="Eye" />
                </Button>
            </DockPanel>


            <Button
                x:Name="btnLightTheme"
                BorderThickness="0"
                Click="btnLightTheme_Click">
                <iconPacks:PackIconModern
                    Width="20"
                    Height="20"
                    Kind="WeatherSun" />
            </Button>

            <Button
                x:Name="btnDarkTheme"
                BorderThickness="0"
                Click="btnDarkTheme_Click">
                <iconPacks:PackIconModern
                    Width="20"
                    Height="20"
                    Kind="MoonWaningCrescent" />
            </Button>

            <Button
                x:Name="btnSyncOSTheme"
                BorderThickness="0"
                Click="btnSyncOSTheme_Click">
                <iconPacks:PackIconModern
                    Width="20"
                    Height="20"
                    Kind="OsWindows" />
            </Button>

            <!--<ComboBox
                BorderThickness="0"
                ItemsSource="{Binding Languages}"
                SelectedValue="{Binding SelectedLanguage}" />-->
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <!--  Pass style into children  -->
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <md:Card
            Grid.Row="0"
            Grid.Column="1"
            Margin="3,3,0,0"
            Background="{DynamicResource MahApps.HamburgerMenu.Pane.Background}"
            Foreground="{DynamicResource MahApps.HamburgerMenu.Pane.Foreground}">

            <DockPanel>

                <DockPanel
                    Grid.Row="0"
                    Width="47"
                    Background="{DynamicResource LabelBuilder_Brush_Active50}">
                    <iconPacks:PackIconMaterial
                        Width="32"
                        Height="32"
                        HorizontalAlignment="Center"
                        Kind="Barcode" />
                </DockPanel>

                <DockPanel
                    Height="32"
                    HorizontalAlignment="Stretch"
                    DockPanel.Dock="Top">
                    <DockPanel.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Offset="0" Color="{DynamicResource LabelBuilder_Color_Active50}" />
                            <GradientStop Offset="1" Color="{DynamicResource MahApps.Colors.HamburgerMenu}" />
                        </LinearGradientBrush>
                    </DockPanel.Background>

                    <TextBlock
                        Margin="6,0,6,3"
                        VerticalAlignment="Center"
                        Style="{StaticResource MahApps.Styles.TextBlock.HamburgerMenuHeader}"
                        Text="Label Builder" />
                </DockPanel>


                <ToolBarTray Grid.Row="1">
                    <ToolBar Padding="0,3" ToolBarTray.IsLocked="True">

                        <Button
                            Command="{Binding BarcodeBuilderViewModel.GenerateBarcodeCommand}"
                            Style="{StaticResource ToolBarButton}"
                            ToolTip="New Database">

                            <DockPanel>
                                <TextBlock
                                    Margin="0,0,0,3"
                                    HorizontalAlignment="Center"
                                    DockPanel.Dock="Top"
                                    Text="Generate"
                                    Visibility="{Binding AppSettings.ShowButtonText, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                <iconPacks:PackIconMaterial
                                    Width="24"
                                    Height="24"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Kind="BarcodeScan" />
                            </DockPanel>
                        </Button>

                        <Button
                            Command="{Binding BarcodeBuilderViewModel.StoreNewBarcodeEntryCommand}"
                            Style="{StaticResource ToolBarButton}"
                            ToolTip="New Database">

                            <DockPanel>
                                <TextBlock
                                    Margin="0,0,0,3"
                                    HorizontalAlignment="Center"
                                    DockPanel.Dock="Top"
                                    Text="Store"
                                    Visibility="{Binding AppSettings.ShowButtonText, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                <iconPacks:PackIconMaterial
                                    Width="24"
                                    Height="24"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Kind="Store" />
                            </DockPanel>
                        </Button>

                    </ToolBar>
                </ToolBarTray>

            </DockPanel>

        </md:Card>

        <DockPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="3,0,3,0">
            <ScrollViewer
                Margin="0,0,3,0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <barcodeViews:ZintSettingsView
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding BarcodeBuilderViewModel}"
                    SectionContainerStyle="{StaticResource LabelBuilderSectionContainerStyle}"
                    SectionExpanderStyle="{StaticResource LabelBuilderSectionExpanderStyle}" />
            </ScrollViewer>

            <ScrollViewer
                Margin="0,0,3,0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <barcodeViews:BarcodeManipulationView
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding BarcodeBuilderViewModel.BarcodeManipulationViewModel}"
                    SectionContainerStyle="{StaticResource LabelBuilderSectionContainerStyle}"
                    SectionExpanderStyle="{StaticResource LabelBuilderSectionExpanderStyle}" />
            </ScrollViewer>

            <ScrollViewer
                Margin="0,0,3,0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <barcodeViews:BarcodeBuilderImagesView
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding BarcodeBuilderViewModel.BarcodeBuilderImagesViewModel}"
                    SectionContainerStyle="{StaticResource LabelBuilderSectionContainerStyle}" />
            </ScrollViewer>

            <ScrollViewer
                Margin="0,0,3,0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <barcodeViews:BarcodesView
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding BarcodeBuilderViewModel.DisplayEditorViewModel}"
                    SectionContainerStyle="{StaticResource LabelBuilderSectionContainerStyle}"
                    SectionExpanderStyle="{StaticResource LabelBuilderSectionExpanderStyle}" />
            </ScrollViewer>

            <ScrollViewer
                Margin="0,0,3,0"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <barcodeViews:DisplayEditorView
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    DataContext="{Binding BarcodeBuilderViewModel.DisplayEditorViewModel}"
                    SectionContainerStyle="{StaticResource LabelBuilderSectionContainerStyle}"
                    SectionExpanderStyle="{StaticResource LabelBuilderSectionExpanderStyle}" />
            </ScrollViewer>

        </DockPanel>

        <StatusBar
            Grid.Row="2"
            Grid.Column="1"
            Margin="0,3,0,0">
            <TextBlock Text="{Binding BarcodeBuilderViewModel.OutputErrorText}" />
        </StatusBar>

    </Grid>
</mah:MetroWindow>
