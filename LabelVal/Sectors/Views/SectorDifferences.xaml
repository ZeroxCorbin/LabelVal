<UserControl
    x:Class="LabelVal.Sectors.Views.SectorDifferences"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converters="clr-namespace:LabelVal.Converters"
    xmlns:ViewModels="clr-namespace:LabelVal.Sectors.Interfaces"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Sectors.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DataContext="{d:DesignInstance Type=ViewModels:ISectorDetails}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Converters:FloatStringTrimConverter x:Key="floatTrim" />
        <BooleanToVisibilityConverter x:Key="booltovis" />
        <Converters:StringCamelUnspaced2StringPascalSpaced x:Key="stringToUpper" />
    </UserControl.Resources>

    <md:Card
        Margin="3"
        Padding="0"
        Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
        Foreground="{DynamicResource MahApps.Brushes.Text}">
        <DockPanel Margin="3">

            <StackPanel>

                <TextBlock Margin="3,0,3,3" Text="{Binding UserName, Mode=OneWay}" />

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Alarms.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding Alarms}">
                        <ItemsControl.ItemTemplate>
                            <HierarchicalDataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                        <TextBlock Text=": " />
                                        <TextBlock Grid.Column="1" Text="{Binding Path=Data.SubAlarm, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text=" ( " />
                                        <TextBlock Text="{Binding Path=Data.Text, StringFormat=N2, Converter={StaticResource floatTrim}, Mode=OneWay}" />
                                        <TextBlock Text="/" />
                                        <TextBlock Text="{Binding Path=Data.Expected, StringFormat=N2, Converter={StaticResource floatTrim}, Mode=OneWay}" />
                                        <TextBlock Text=" )   " />

                                        <Border
                                            Width="10"
                                            Height="10"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Background" Value="{DynamicResource CB_Green}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Category, Mode=OneWay}" Value="2">
                                                            <Setter Property="Background" Value="{DynamicResource SectorError_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Category, Mode=OneWay}" Value="1">
                                                            <Setter Property="Background" Value="{DynamicResource SectorWarning_Brush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </StackPanel>

                                </Grid>
                            </HierarchicalDataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding GradeValues.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding GradeValues}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="3,0,6,0" Orientation="Horizontal">

                                        <TextBlock Grid.Column="0" Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Value, Mode=OneWay, StringFormat={}({0:N1})}" />

                                        <TextBlock Grid.Column="1" Text="{Binding Path=Grade.Value, StringFormat=N1, Mode=OneWay}" />
                                        <TextBlock Text=" " />

                                        <Border
                                            Width="10"
                                            Height="10"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Background" Value="{DynamicResource CB_Green}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Grade.Letter, Mode=OneWay}" Value="B">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeB_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Grade.Letter, Mode=OneWay}" Value="C">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeC_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Grade.Letter, Mode=OneWay}" Value="D">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeD_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Grade.Letter, Mode=OneWay}" Value="F">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeF_Brush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{Binding Path=Grade.Letter, Mode=OneWay}" />

                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Values.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding Values}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="3,0,6,0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Value, Mode=OneWay, StringFormat={}{0:N0}}" />
                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ValueResults.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding ValueResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="3,0,6,0" Orientation="Horizontal">
                                        <TextBlock Grid.Column="0" Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Value, Mode=OneWay, StringFormat={}({0:N1})}" />

                                        <Border
                                            Width="10"
                                            Height="10"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Background" Value="{DynamicResource CB_Green}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Path=Result, Mode=OneWay}" Value="FAIL">
                                                            <Setter Property="Background" Value="{DynamicResource SectorError_Brush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{Binding Path=Result, Mode=OneWay}" />

                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Gs1ValueResults.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding Gs1ValueResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="3,0,6,0" Orientation="Horizontal">
                                        <TextBlock Grid.Column="0" Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Value, Mode=OneWay, StringFormat={}({0:N2})}" />

                                        <Border
                                            Width="10"
                                            Height="10"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Background" Value="{DynamicResource CB_Green}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Path=Result, Mode=OneWay}" Value="FAIL">
                                                            <Setter Property="Background" Value="{DynamicResource SectorError_Brush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{Binding Path=Result, Mode=OneWay}" />

                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Gs1Grades.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <!--<DataTrigger Value="False" Binding="{Binding IsGS1Standard}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>-->
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding Gs1Grades}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Margin="3,0,6,0" Orientation="Horizontal">

                                        <TextBlock Grid.Column="0" Text="{Binding Path=Name, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Value, Mode=OneWay, StringFormat={}({0:N1})}" />

                                        <TextBlock Text=" " />
                                        <Border
                                            Width="10"
                                            Height="10"
                                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                                            BorderThickness="1"
                                            CornerRadius="3">
                                            <Border.Style>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="Background" Value="{DynamicResource CB_Green}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Letter, Mode=OneWay}" Value="B">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeB_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Letter, Mode=OneWay}" Value="C">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeC_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Letter, Mode=OneWay}" Value="D">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeD_Brush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Letter, Mode=OneWay}" Value="F">
                                                            <Setter Property="Background" Value="{DynamicResource ISO_GradeF_Brush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{Binding Path=Letter, Mode=OneWay}" />
                                    </StackPanel>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel>
                    <DockPanel.Style>
                        <Style TargetType="{x:Type DockPanel}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Blemishes.Count, Mode=OneWay}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                    <Separator DockPanel.Dock="Top" />
                    <ItemsControl ItemsSource="{Binding Blemishes, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <HierarchicalDataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Path=Type, Converter={StaticResource stringToUpper}, Mode=OneWay}" />
                                    </StackPanel>

                                </Grid>
                            </HierarchicalDataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </DockPanel>

                <DockPanel Visibility="{Binding IsNotOCVMatch, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Separator DockPanel.Dock="Top" />
                    <TextBlock Margin="2" Text="{Binding OCVMatchText}" />
                </DockPanel>

                <DockPanel Visibility="{Binding IsSectorMissing, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Separator DockPanel.Dock="Top" />
                    <TextBlock Margin="2" Text="{Binding SectorMissingText}" />
                </DockPanel>

            </StackPanel>

        </DockPanel>

    </md:Card>

</UserControl>
