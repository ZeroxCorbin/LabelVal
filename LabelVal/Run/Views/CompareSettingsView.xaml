<UserControl
    x:Class="LabelVal.Run.Views.CompareSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    xmlns:ViewModels="clr-namespace:LabelVal.Run.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Dialog:DialogParticipation.Register="{Binding}"
    d:DataContext="{d:DesignInstance Type=ViewModels:CompareSettingsViewModel}"
    d:DesignHeight="260.592"
    d:DesignWidth="417.861"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <ViewModels:CompareSettingsViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style
            x:Key="ConditionalTextBoxStyle"
            BasedOn="{StaticResource {x:Type TextBox}}"
            TargetType="{x:Type TextBox}">
            <Setter Property="IsEnabled" Value="False" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType={x:Type CheckBox}}}" Value="True">
                    <Setter Property="IsEnabled" Value="True" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border
        Margin="5"
        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
        BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Graded Values  -->
            <GroupBox
                Grid.Row="0"
                Margin="3"
                Header="Graded Values">
                <StackPanel>
                    <CheckBox Content="Use Letter" IsChecked="{Binding GradeValue_UseGradeLetter}" />
                    <TextBox Style="{StaticResource ConditionalTextBoxStyle}" Text="{Binding GradeValue_GradeValueTolerance}" />
                    <CheckBox Content="Use Value" IsChecked="{Binding GradeValue_UseValue}" />
                    <TextBox Style="{StaticResource ConditionalTextBoxStyle}" Text="{Binding GradeValue_ValueTolerance}" />
                </StackPanel>
            </GroupBox>

            <!--  GS1 Results  -->
            <GroupBox
                Grid.Row="1"
                Margin="3"
                Header="GS1 Results">
                <StackPanel>
                    <CheckBox Content="Use Result" IsChecked="{Binding ValueResult_UseResult}" />
                    <TextBox Style="{StaticResource ConditionalTextBoxStyle}" Text="{Binding ValueResult_ValueTolerance}" />
                </StackPanel>
            </GroupBox>

            <!--  GS1 Grades  -->
            <GroupBox
                Grid.Row="2"
                Margin="3"
                Header="GS1 Grades">
                <StackPanel>
                    <CheckBox Content="Use Letter" IsChecked="{Binding Grade_UseGradeLetter}" />
                    <TextBox Style="{StaticResource ConditionalTextBoxStyle}" Text="{Binding Grade_GradeValueTolerance}" />
                </StackPanel>
            </GroupBox>
        </Grid>
    </Border>
</UserControl>