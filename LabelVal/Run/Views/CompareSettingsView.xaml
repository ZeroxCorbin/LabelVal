<UserControl
    x:Class="LabelVal.Run.Views.CompareSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converters="clr-namespace:LabelVal.Converters"
    xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    xmlns:V275Converters="clr-namespace:LabelVal.V275.Converters"
    xmlns:ViewModels="clr-namespace:LabelVal.Run.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:LabelVal.Run.Views"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Dialog:DialogParticipation.Register="{Binding}"
    d:DataContext="{d:DesignInstance Type=ViewModels:CompareSettingsViewModel}"
    d:DesignHeight="260.592"
    d:DesignWidth="417.861"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <ViewModels:CompareSettingsViewModel />
    </UserControl.DataContext>
    <Border
        Grid.Row="0"
        Grid.Column="0"
        Margin="5"
        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
        BorderThickness="2">
        <Grid>
            <StackPanel>



                <Label Content="Graded Values" />
                <Border
                    Margin="3"
                    BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                    BorderThickness="2">
                    <StackPanel>
                        <CheckBox Content="Use Letter" IsChecked="{Binding GradeValue_UseGradeLetter}" />
                        <TextBox Text="{Binding GradeValue_GradeValueTolerance}">
                            <TextBox.Style>
                                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding GradeValue_UseGradeLetter}" Value="True">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <CheckBox Content="Use Value" IsChecked="{Binding GradeValue_UseValue}" />
                        <TextBox Text="{Binding GradeValue_ValueTolerance}">
                            <TextBox.Style>
                                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding GradeValue_UseValue}" Value="False">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </StackPanel>
                </Border>

                <Label Content="GS1 Results" />
                <Border
                    Margin="3"
                    BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                    BorderThickness="2">
                    <StackPanel>
                        <CheckBox Content="Use Result" IsChecked="{Binding ValueResult_UseResult}" />
                        <TextBox Text="{Binding ValueResult_ValueTolerance}">
                            <TextBox.Style>
                                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ValueResult_UseResult}" Value="True">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </StackPanel>
                </Border>


                <Label Content="GS1 Grades" />
                <Border
                    Margin="3"
                    BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                    BorderThickness="2">
                    <StackPanel>
                        <CheckBox Content="Use Letter" IsChecked="{Binding Grade_UseGradeLetter}" />
                        <TextBox Text="{Binding Grade_GradeValueTolerance}">
                            <TextBox.Style>
                                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type TextBox}">
                                    <Setter Property="IsEnabled" Value="True" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Grade_UseGradeLetter}" Value="True">
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
