<mah:MetroWindow
    x:Class="LabelVal.ORM_Test.NHibernateSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:json="clr-namespace:JSONViewer_WPF;assembly=JSONViewer_WPF"
    xmlns:local="clr-namespace:LabelVal.ORM_Test"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:LabelVal.ORM_Test"
    Height="600"
    d:DataContext="{d:DesignInstance Type=vm:NHibernateSettingsViewModel}"
    SizeToContent="Width"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <mah:MetroWindow.DataContext>
        <vm:NHibernateSettingsViewModel />
    </mah:MetroWindow.DataContext>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Border
                Margin="5"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="2">
                <ListView ItemsSource="{Binding SQLConfigurations}" SelectedValue="{Binding SQLConfiguration}" />
            </Border>

            <Border
                Margin="5"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="2">
                <StackPanel>
                    <StackPanel Margin="3" Orientation="Horizontal">
                        <Label MinWidth="100" Content="Server Address" />
                        <TextBox MinWidth="100" Text="{Binding Server}" />
                    </StackPanel>

                    <StackPanel Margin="3" Orientation="Horizontal">
                        <Label MinWidth="100" Content="Port Number" />
                        <TextBox MinWidth="100" Text="{Binding Port}" />
                    </StackPanel>

                    <StackPanel Margin="3" Orientation="Horizontal">
                        <Label MinWidth="100" Content="Database Name" />
                        <TextBox MinWidth="100" Text="{Binding Database}" />
                    </StackPanel>

                    <StackPanel Margin="3" Orientation="Horizontal">
                        <Label MinWidth="100" Content="User Name" />
                        <TextBox MinWidth="100" Text="{Binding Username}" />
                    </StackPanel>

                    <StackPanel Margin="3" Orientation="Horizontal">
                        <Label MinWidth="100" Content="Password" />
                        <TextBox MinWidth="100" Text="{Binding Password}" />
                    </StackPanel>

                    <!--<Button Command="{Binding SaveSettingsCommand}" Content="Save"/>-->
                </StackPanel>
            </Border>

            <Border
                Margin="5"
                BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                BorderThickness="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <StackPanel
                        Grid.Row="0"
                        Margin="3"
                        Orientation="Horizontal">
                        <Label MinWidth="100" Content="SQL Statement" />
                        <Border
                            Margin="5"
                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                            BorderThickness="2">
                            <TextBox
                                Width="600"
                                Text="{Binding SQLStatement}"
                                TextWrapping="Wrap" />
                        </Border>
                    </StackPanel>

                    <StackPanel
                        Grid.Row="1"
                        Margin="3"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <Button
                            Width="80"
                            Margin="3"
                            Command="{Binding ExecuteStatement}"
                            Content="Execute" />
                        <CheckBox Content="Fix JSON" IsChecked="{Binding FixJSON}" />
                    </StackPanel>

                    <StackPanel
                        Grid.Row="2"
                        Margin="3"
                        Orientation="Horizontal">
                        <Label MinWidth="100" Content="SQL Response" />
                        <!--<json:JsonViewer JSON="{Binding SQLResponse}" Width="600" TitleVisible="False"/>-->
                        <Border
                            Margin="5"
                            BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
                            BorderThickness="2">
                            <DataGrid
                                MaxWidth="1024"
                                ItemsSource="{Binding DataTable}"
                                MaxColumnWidth="420" />
                        </Border>
                    </StackPanel>


                </Grid>
            </Border>
        </StackPanel>

    </Grid>
</mah:MetroWindow>
