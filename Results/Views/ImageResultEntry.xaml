<UserControl
    x:Class="LabelVal.Results.Views.ImageResultEntry"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LabelVal.Results.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:LabelVal.Results.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:ImageResultEntry,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:ImageResultDeviceEntry_V275}">
            <local:ImageResultDeviceEntryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ImageResultDeviceEntry_V5}">
            <local:ImageResultDeviceEntryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ImageResultDeviceEntry_L95}">
            <local:ImageResultDeviceEntryView />
        </DataTemplate>
    </UserControl.Resources>

    <Border BorderBrush="Black" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <local:ImageResultEntry_Images Grid.Row="0" />

            <ItemsControl
  
                Grid.Column="1"
                ItemsSource="{Binding ImageResultDeviceEntries}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <DockPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

        </Grid>
    </Border>
</UserControl>